export const slugs = {
  contact: 'contact',
  home: '',
  writing: 'writing',
};

export const paths = {
  contact: `/${slugs.contact}`,
  home: `/`,
  writing: `/${slugs.writing}`,
};

export const BASE_URL = 'https://moira-barrett.com';
