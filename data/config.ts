export const slugs = {
  contact: 'contact',
  home: '',
  writing: 'writing',
  cv: 'cv',
};

export const paths = {
  contact: `/${slugs.contact}`,
  home: `/`,
  writing: `/${slugs.writing}`,
  cv: `/${slugs.cv}`,
};

export const BASE_URL = 'https://moira-barrett.com';
